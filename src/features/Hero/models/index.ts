export interface HeroData {
    firstName: string;
    lastName: string;
    role: string;
    description: string;
    techStack: string;
    cvLink: string;
    status: string;
}

export interface HeroProps {
    theme: string;
}
